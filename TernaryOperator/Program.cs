using System;

namespace TernaryOperator
{
    class Program
    {
        static void Main(string[] args)
        {
            int a = 90;
            string b = (a > 50) ? "A is greater." : "A is smaller.";
            Console.WriteLine(b);
        }
    }
}
